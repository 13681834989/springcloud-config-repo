management:
  security:
    enabled: false


#发生超时是是否中断，默认true
hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: true
        isolation:
          thread:
            timeoutInMilliseconds: 50000

hystrix:
  config:
    stream:
      maxConcurrentConnections: 50